rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo-friendly rules for ENG 1102 prototype (tighten after Auth is enabled).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
